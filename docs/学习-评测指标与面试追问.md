# 学习笔记 · 评测指标与面试追问应对

> 这份文档写给项目作者自己复习用：把 README 评测节背后每一个数字、每一个可能被面试官追问的问题，掰开揉碎讲清楚。读文档建议顺序：先读 §1 打底，再读 §2 消融，§3-§5 是三个"必被追问"的预案。

---

## 1. 评测指标到底是什么（通俗版）

### 1.1 Hit@5 —— 找没找到

把问题丢进检索器，它返回排好序的资料列表。**Hit@5 问的是：正确的那份资料有没有出现在前 5 条里。** 出现了算"命中"，没出现算"漏了"。

比喻：在图书馆找"武器价格"，管理员抱来一摞书，前 5 本里有没有你要的那本？有 = 命中。

本项目：17 个检索题里 12 个命中 → 70.6%。

### 1.2 MRR —— 排得靠不靠前（重点，也是你最容易讲错的地方）

MRR = **Mean Reciprocal Rank，平均倒数排名**。它不是精确率！

规则：正确资料排第 1 名记 1 分，排第 2 名记 1/2 = 0.5 分，排第 3 名记 1/3 ≈ 0.33 分……越靠后分越低，没进前 5 记 0 分。所有题目的得分取平均就是 MRR。

比喻：同样是"找到了"，管理员第 1 本就递给你（1 分）和翻了 4 本才递给你（0.25 分），体验完全不同。MRR 衡量的就是这个"第几本就递到"。

**为什么 MRR 从 0.882 降到 0.796（旧数据）/ 0.637 降到 0.598（消融数据）？**

旧管线（纯向量）几乎每题的正确资料都排第 1（分数接近"满分 1.0"）；开了混合检索后，BM25 这个"关键词搜索器"会把一些**沾边但不核心**的资料也拉进前排——比如教程文档里顺带提了一句 Jett，用户问 Jett 时它也冲到了前几名。而入门级的重排模型纠不动这种错位，于是正确资料的"名次"平均被挤后了一点，MRR 就降了。

注意：**MRR 降 ≠ 找不到（Hit@5 没降）≠ 答错**。它只是"正确答案的名次平均往后挪了 1-2 位"。

### 1.3 精确率 vs 召回率 —— 你的"召回比精确重要"对不对？

先分清三个概念（用"捞鱼"比喻）：

| 指标 | 问的问题 | 漏捞鱼？ | 捞到垃圾？ |
|------|------|------|------|
| **召回率**（Recall） | 该找的都找到了吗？ | 扣分 | 不扣 |
| **精确率**（Precision） | 找到的里有用吗？ | 不扣 | 扣分 |
| **MRR** | 最好的排第几？ | 排名靠后扣分 | 挤占前排扣分 |

你说"**召回比精确重要，宁愿精确率低一些也要召回到位**"——方向是对的，而且是检索工程的常见取舍：**宁可多捞一些交给后面过滤，也别一开始就漏**。混合检索的设计动机正是这个：BM25 兜住纯向量可能漏掉的关键词命中。

但要补一层：在 RAG 系统里"宁多勿漏"只在**检索层**成立，因为后面还有重排序帮忙过滤；到了**最终答案层**，规则反过来了——**"答错"比"不答"严重得多**，所以拒答兜底（宁可说"我不知道"也不瞎编）是精确率思维的胜利。一句话总结：**检索层保召回，答案层保精确，两层分工不同。**

（另外提醒：你说的"失主的自我判断最准确"是失物招领系统里的逻辑——那是人对人的匹配确认。RAG 项目里没有失主，面试时注意别把两个项目的概念讲串了，这两个项目都是你简历上的，面试官很可能两个都问。）

### 1.4 "75%（24/32）"里的 32 是什么

32 = **评测集里所有题目"期望关键词"的总个数**。

评测集每道题除了指定"应该命中哪份文档"（expected_docs），还列了"答案里应该出现哪些词"（expected_keywords）。比如题目"正义手枪多少钱？伤害是多少？"期望关键词是 `["800", "55", "159"]` 三个词。把 17 道生成题的关键词全加起来 = 32 个。

开启 LLM 生成后，看答案文本里实际包含了其中几个词：命中 24 个 → 24/32 = 75%。它衡量"答得全不全"，是生成质量的粗代理（不是智能评测，只是关键词命中，所以它高也不代表答案通顺）。

2026-09-18 已用新模型 GLM-4.7-Flash 复测：20/32 = **62.5%**，比 GLM-4-Flash 时期的 75.0% 低 12.5 个百分点。这不代表模型变笨——关键词覆盖是"逐字命中"式的粗判分，不同模型措辞风格不同（新模型更爱换同义表述），同一评测集对比不同模型时要记住这个口径局限。复跑命令：`python scripts/evaluate_rag.py --mode hybrid`（不带 `--skip-llm`）。

### 1.5 为什么 94.1% 变成了 70.6% —— 数字漂移的活教训

README 旧版写 Hit@5 94.1%，今天实测 70.6%（12/17）。两个原因：

1. **口径**：旧数字的分母其实是 17 不是 20（3 道拒答题只算拒答正确率，不进检索指标）——但旧 README 没写口径，读者会拿 20 当分母去猜；
2. **知识库变过**：评测数字依赖知识库内容。9 月中知识库 12 篇文档都改过内容，向量排序随之变化，旧数字就作废了。

教训写进 README 了：**评测数字必须带日期和知识库版本，改库必须重跑**。这也是面试加分点——你能主动讲"我的评测数字会失效，所以我标注了评测环境"，比背一个漂亮数字强得多。

---

## 2. 消融实验：每段管线到底有没有用

### 2.1 什么是消融实验

把系统的零件一个个关掉，看指标掉不掉：关掉掉分 = 这零件有用；关掉不掉分 = 这零件是摆设（或者它的价值场景没被测到）。比喻：拔掉一根线看灯灭不灭，灭了才说明灯是它点的。

### 2.2 实测结果（2026-09-18，skip-llm）

| 组合 | Hit@5 | MRR | 拒答正确率 |
|---|---|---|---|
| A. 纯向量 | 70.6% | 0.637 | 33% |
| B. 纯向量+重排 | 70.6% | 0.608 | **100%** |
| C. 混合检索 | 70.6% | 0.637 | 0% |
| D. 混合+重排（v3） | 70.6% | 0.598 | **100%** |

### 2.3 三条结论（面试可以直接讲）

1. **重排序是拒答能力的唯一来源**：B、D 都是 100%，A、C 最高 33%。机制：CrossEncoder 的分数区分度好（无关 ≈0.50，相关 ≥0.67），0.60 阈值能切开；原始向量相似度区分度差，切不开。
2. **混合检索在当前知识库规模下零检索收益**：Hit@5 一分没涨，MRR 还被 BM25 的弱关联块拖低 0.039。保留它的理由是给未来知识库扩容预留架构，不是因为现在有用。
3. **查询改写测不出效果是评测集的局限**：单轮问题没有指代可消解，它的战场在多轮对话——这套评测集压根没考它。面试被问"改写有什么用"时，要主动说"我的单轮评测覆盖不到它，这是评测集盲区"。

### 2.4 这组消融的最佳用途

面试官若质疑"三级管线是不是过度设计"，标准答案就是这张表：**"我做了消融，拒答收益全部来自重排；混合检索是架构预留；我也因此知道该优先优化什么（换更强 reranker / 扩评测集），而不是无脑堆模块。"** 敢承认哪段没用，比吹全都有用可信十倍。

---

## 3. 拒答阈值 0.60：怎么调的，上线后怎么监控

### 3.1 现状与风险（先自曝）

0.60 来自：重排分数分布中，无关问题 ≈0.50、相关问题 ≥0.67，取中间。**风险**：这组观察来自评测集那 20 题，阈值在它上面调、又在它上面报 100%——"用考卷练题再考同一张卷"，换一批新题分数可能漂移。这是这套评测最大的方法学软肋，面试前必须心里有数。

### 3.2 短期可做的三步（不用改架构）

1. **切分验证集**：把评测集按 7:3 分成"调参集"和"验证集"——阈值只在调参集上调，数字在验证集上报。哪怕只有十几题，也比自证循环强；
2. **记录分数分布**：评测脚本已输出每题 top 来源和分数，把"相关/无关"两组分数画成直方图，确认 0.60 落在两坨分布的中间谷地，而不是切在某一坨的山坡上；
3. **边界用例回归**：故意准备 5-10 个"半相关"问题（比如"CS 的 AK 多少钱"），看分数落在哪，确认边界行为符合预期。

### 3.3 上线后的监控方案（面试话术）

- **埋点**：每次问答记录"重排最高分 + 是否拒答"两个字段（审计日志里已有基础）；
- **看分布漂移**：按天/周聚合最高分分布。健康状态是双峰（相关一坨、无关一坨，中间隔着阈值）；如果分布整体移动（比如用户问起新版本英雄，措辞变了），峰形会偏移；
- **漂移警报**：拒答率突然飙升（分布整体左移）或归零（阈值失效）都报警；
- **定期回归**：知识库每次更新后重跑评测集，数字与上次对比，跌幅超 5 个百分点即人工审查。

---

## 4. 企业要"文档级权限"怎么办

### 4.1 先分清两件事

- **kb_id 隔离（本项目已有）**：不同业务/租户各建各的知识库，A 部门查不到 B 部门的库。这是**库与库之间**的隔离。
- **文档级权限（本项目没有）**：同一个库里，"薪资制度.pdf"只有 HR 能查，普通员工查"薪资"时根本不该检索到它。这是**库内文档与文档之间**的隔离。

面试时如果只答 kb_id，懂行的会立刻追问这一层。标准答案是承认差距 + 给出方案：

### 4.2 方案（从易到难三层）

1. **元数据过滤（第一层，改动最小）**：入库时给每个文档块打权限标签（`dept=hr`、`level=manager`），查询时先取用户权限，把过滤条件拼进向量检索的 where 子句（ChromaDB 支持 metadata where 过滤）。缺点：过滤逻辑自己维护，标签一多就乱。
2. **外部权限源对齐（第二层，生产主流）**：企业权限通常已在 AD/LDAP/OA 系统里。检索时先问权限系统"这个用户能看哪些文档 ID"，再把允许清单传给检索器做过滤（类似大厂 RAG 里常见的 ACL 管道）。关键原则：**权限判断永远以权限系统为准，RAG 只做执行者**，否则权限更新后向量库会滞后。
3. **多租户/多集合（第三层，重隔离）**：敏感等级极高的文档单独建 collection 甚至独立向量库实例，物理隔离，宁可牺牲一点检索的跨域召回。

配套还要答两点：**审计**（谁在什么时候查到了什么，本项目 audit 日志已有雏形）和**索引侧安全**（文档删除/权限收紧后，旧向量必须同步删掉，否则"人看不见了但 AI 还能搜出来"是最常见的事故）。

---

## 5. "降低幻觉"缺的证据与补法

README 卖点之一是"降低幻觉"，但目前的证据只有间接的：检索有依据（Hit@5）、答不出就拒答（100%）、答案带来源。**缺的是直接证据：答案本身有多少比例忠于检索到的资料**——这个指标叫 **faithfulness（忠实度）**。

补法（工作量从小到大）：

1. **人工抽检**：拿 30 条真实回答，人工判断每条是否与来源一致，给个忠实率。半小时的活，聊胜于无；
2. **RAGAS 自动评测**：开源的 RAG 评测框架，用 LLM 给"忠实度 / 答案相关性 / 上下文相关性"打分。需要有效的 LLM 密钥；
3. **引用对齐校验**：要求生成时每句话标注引用编号，程序校验"这句话的信息是否真的出现在被引文档块里"。工程量最大，但最硬核。

面试话术："当前用机制（检索依据 + 拒答 + 来源展示）压幻觉，量化忠实度评测是 roadmap 项，计划用 RAGAS 补。"

---

## 6. 快速自测（读完合上文档，能答出来才算过）

1. MRR 0.598 和 Hit@5 70.6% 分别在说什么？MRR 降了说明找不到吗？
2. 消融表里哪一格数据证明"拒答收益全部来自重排序"？
3. 为什么说阈值 0.60 有自证循环风险？三步补救是什么？
4. kb_id 隔离和文档级权限差在哪？生产上权限判断以谁为准？
5. 你的评测数字 70.6% 为什么不能写进简历后就不管了？

---

## 7. 模型切换记：GLM-4-Flash → GLM-4.7-Flash（2026-09-18，真实踩坑实录）

### 7.1 先澄清一个误会：key 一直都在用，不是"没激活"

你的疑问是"如果 LLM 一直没激活，之前它怎么能回答我说话？"——答案是：**它一直都在正常用**。这个项目的每条回答都是"检索 + LLM 生成"两步，没有 LLM 一句话都答不出来。真实的时间线是：

1. **9 月初～中**：key 有效，项目一直能正常回答（你看到的所有回答都经过 GLM）；
2. **某时点开始 401（鉴权失败）**：最常见原因是免费额度/配额用尽或账号欠费被暂停服务——免费模型不扣钱，但**账号欠费时平台会停掉整个账号的调用权限**，连免费模型也调不了（这就是"充了 10 块钱就好了"的原因）；
3. **2026-09-18 充值后**：立即恢复，实测 GLM-4.7-Flash 正常返回。

所以"激活"这个词不准确——key 从创建起就是激活的，需要的是**有效 + 账号不欠费**。

### 7.2 踩坑一：混合思考模型会把答案"藏"进思考字段

GLM-4.7-Flash 是"混合思考模型"（30B 总参 / 3B 激活的 MoE 架构，2026-01 上线免费）。直接调用它会遇到一个诡异现象：

- 返回的 `reasoning_content` 字段里有一大段思考过程；
- 而 `content`（正式答案字段）是**空的**。

本项目所有取答案的代码都读 `content`，等于答案全被"藏"进了思考字段——界面上就是答非所问或空白。解法：请求体里显式传 `{"thinking": {"type": "disabled"}}` 关闭思考模式，`content` 就正常了。

**为什么本项目默认关思考而不是展示思考**：RAG 的价值是"快 + 有依据"，答案已经在检索到的资料里，不需要模型再推理一轮；开思考会多花 token、慢好几秒，还会污染评测（拒答判定靠关键词匹配拒答话术，思考文本混进答案会误判）。想要深度推理时，`.env` 里设 `LLM_THINKING=1` 即可打开。（v3.4 最终默认策略：查询改写**开**思考提精度；质量自评经 Layer3 标注集 A/B 实测开/关判分均 10/10、思考零增益后**关**；最终答案生成**关**。三环节均可环境变量独立调整。）

### 7.3 踩坑二：免费模型的 429 限流

免费模型高峰期会返回 429（"该模型当前访问量过大"），实测三次调用能连出两次。对策两层：

- **代码层**：`_call_llm_with_retry` 重试等待从固定 1 秒改为递增退避（3 秒 → 6 秒），给限流窗口留出恢复时间；
- **使用层**：答案长度设 `LLM_MAX_TOKENS=512` 能明显缩短占用，错峰使用最稳。

### 7.4 面试怎么讲这次切换

一句话版本："我把生成模型从 GLM-4-Flash 升级到免费的 GLM-4.7-Flash，踩了两个坑并都解决了：混合思考模型默认把答案写进 reasoning_content 导致界面空白，我在请求体里显式关闭思考模式；免费档 429 限流，我把重试改成递增退避。模型升级没有改一行检索管线代码——这也验证了 LLM 在 RAG 里是可插拔组件。"

（最后一个点最重要：RAG 架构的工程价值之一就是**生成模型可替换**，你这次切换就是活证据。）

## 8. 快速自测（第 7 节加两题）

6. 换成 GLM-4.7-Flash 后界面答非所问，第一件事查什么？（答：content 是否为空、reasoning_content 是否有内容——思考模式没关）
7. 免费模型为什么不扣钱还会被停？（答：免费的是模型调用费，账号欠费会被平台整体暂停服务）

---

## 9. v3.3 升级笔记：GPU 混合模式与质量自评的设计思路（2026-09-20）

### 9.1 设备策略：为什么用 mem_get_info 而不是 memory_allocated

- `torch.cuda.memory_allocated()` 只统计**本进程**的 torch 显存占用，看不到游戏等其他程序——用它判断"还有没有空闲"会误判；
- `torch.cuda.mem_get_info()` 直接问驱动"整张卡还剩多少空闲"，游戏占了多少一目了然。判断依据应该是它；
- 阈值 1536MB：两个模型（92MB embedding + 约1GB reranker）加运行开销约 1.2GB，留 0.3GB 余量；
- **OOM 是最后防线**：启动时检测得再准，也可能在推理瞬间被游戏抢走显存。所以推理代码里 `except torch.cuda.OutOfMemoryError` → `empty_cache()` → 模型 `.to("cpu")` 重试一次，服务不断；
- **同进同退**：向量模型和重排模型必须在一个设备上，一个 CPU 一个 GPU 会因 PCIe 数据来回拷贝成倍变慢。本项目两个模型本来就共用同一 device 配置，天然满足；
- 前置工程：装 CUDA 版 torch（`torch==2.12.1+cu126`，约2.5GB）。CPU 版 torch 无论代码怎么写都用不了显卡——排查"GPU 不生效"先看 `torch.cuda.is_available()`。

### 9.2 质量自评 Critic：灰区触发与"最坏情况封顶"

- **为什么默认不全量评审**：每道题都多一次带思考的 LLM 调用（3~10秒），大部分问题重排分数很高，评审是浪费。设计成灰区触发：top1 ≥ 0.75 直接放行（零额外延迟），低于拒答阈值 0.60 本来就拒答，只有 [0.60, 0.75) 的灰区才评审；
- **不足时怎么办**：让模型换一个角度重写查询再检索（最多 3 轮），两轮结果取 top1 分更高者——"再试一次"比"直接放弃"多救回一些问题，且最坏延迟有 3 轮封顶；
- **评测与增强分离**：`evaluate_rag.py` 默认 `RAG_CRITIC=0`——评测要度量的是检索管线本身，Critic 是对话层的补偿机制，混进去评测数字就没法归因了。这个"分层归因"思想值得记住：**每层机制单独可开关、单独可评测**；
- **一个真实 bug 的教训**：查询改写模块有自己独立的 LLM 实例，换 glm-4.7 后它没有挂"关思考"包装，content 一直是空的——改写静默失效了好几天，靠"失败回退原问题"的设计才没出事故。教训：**多个模型实例要共享同一套适配逻辑（工厂模式），新来一个实例就多一处漏配风险**。
### 9.3 GPU 精度排障记（一次完整的"对照实验"实战，2026-09-20）

上 GPU 后变体鲁棒性 8 题从 8/8 掉到 5/8，主套件却纹丝不动。排查过程值得记住——这是标准的**对照实验定位法**：

1. **先排除代码回归**：同一份代码、同一个评测集，强制 CPU 复跑 → 8/8 满分。代码没问题，差异只可能来自设备；
2. **怀疑 TF32**（Ampere+ 显卡 matmul 的 10 位尾数低精度模式，重排分数在 0.01~0.05 间扎堆，最怕这种噪声）：用 `NVIDIA_TF32_OVERRIDE=0` 强制关闭复跑 → 7/8。部分解释；
3. **怀疑 sdpa/flash 注意力内核**（GPU 上 transformers 默认换内核算法，不是纯浮点噪声而是不同实现）：`torch.backends.cuda` 里关 flash/mem_efficient、强制 math 路径 → 仍 7/8；
4. **结论**：剩下的 1 题是纯 fp32 归约顺序差异（GPU 与 CPU 累加顺序不同，最后一个浮点位的差别），属于不可完全消除的物理现实。主套件比特级一致，唯一波动的"baotu 拼音"是人工压测的极端边界题；
5. **处置**：TF32/内核开关已写死进 `device_manager`（确定性优先，这两个模型太小，低精度没有速度收益）；文档如实标注"GPU 与 CPU 存在边界题 ±1 的浮动"；实测 GPU 把单条检索从约 900ms 压到约 110ms（约 8~10 倍），花这点代价完全值得。

**方法论收货**：性能问题的排查姿势=固定其他变量、一次只改一个、每步跑同一份评测对照。跳过第 1 步（CPU 复跑排除代码回归）的人，会在错误的层面修一整天。
