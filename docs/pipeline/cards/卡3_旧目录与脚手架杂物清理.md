# 卡 #3：旧数据目录与脚手架杂物清理
- 目标：删除确认废弃的旧数据目录与未引用文件；可留疑的归档不删除。
- 涉及文件（处理清单）：
  - **删除**：根目录 `data/`（7 月旧版 chroma_db + 空 chat_history；删除前必须核实 `backend/data/chroma_db` 存在且非空，且根 data/ 最近无新写入——用文件修改时间判断）；
  - **删除**：`frontend/src/components/ChatPage.vue.bak`；
  - **删除**：`frontend/src/components/HelloWorld.vue`、`TheWelcome.vue`、`WelcomeItem.vue`（先 grep 全前端确认无 import 引用才删）；
  - **归档（移动到 `docs/attic/`，不删除）**：根目录 `网址`、`start_test.log`；
  - **绝对不动**：`aoci*.txt`、`study-cao.skill`、`.workbuddy/`、`.agents/`、`backend/data/` 全部、`.env`。
- 前置依赖：无（与卡#1/#2 无文件交集，可并行）
- 实现要点：每删一项前先做该卡列出的核实动作；删除用 `rm`/`git rm`；归档用 `mkdir -p docs/attic && mv`。
- 基线：清理前 `git status` 贴入卡文件底部（当前未提交改动如有，先报告主会话再动）。
- 验收（怎么算做完）：
  - 启动后端 `GET /docs` 200、前端 `npm run dev` 可起无报错（证明没删错）；
  - 本卡处理清单逐项打勾（删了什么/归档了什么）；
  - `git status` 显示的变更仅限本卡清单内文件。
- 禁止：不清理 backend/ 内部任何文件；不动 .git；不"顺手"格式化任何代码；aoci 与 .skill 文件一个字节都不碰。
- 完成后动作：贴 `git status` 短输出与启动验证结果；更新台账卡#3 行；返回 3 行以内摘要。
