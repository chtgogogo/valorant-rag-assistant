# 卡 #9（RAG）：审计接口挂鉴权 + 死代码双清（安检 L1 修复）
- 背景：安检 L1 三项阻断——①`/api/audit/recent`（main.py 42-43）是全项目唯一未挂 `verify_api_key` 的路由，却存全部用户问答原文；②`chat_service.test_llm_call`（约 30-36 行）参数是 `question` 却传未定义的 `question_for_prompt`，一调用必崩；③前端残留调用该死函数的按钮。
- 涉及文件：`backend/main.py`（一行 dependencies）、`backend/services/chat_service.py`（删 test_llm_call）、前端对应按钮与 api 函数、`CHANGELOG.md`（v3.12）。
- 实现要点：
  1. audit 接口补 `dependencies=[Depends(verify_api_key)]`，与其他六个路由完全同款；
  2. **先核实鉴权放行逻辑**：读 `backend/utils/auth.py` 确认 `AUTH_ENABLED=0`（settings 默认关）时 verify_api_key 直接放行——若是，挂上后本地行为零变化，前端管理页审计表不受影响（把这个逻辑链写进报告）；删除 test_llm_call 整个函数（含注释头）；
  4. 前端：grep 找到调用该接口的按钮与 api 函数，整链删除（按钮+函数+样式残留）；
  5. **产出安检报告 L1 快照**：`docs/pipeline/安检报告.md`，本卡只填第 4（鉴权矩阵）、11（死代码）两项结论+证据，其余项标"待后续卡"。
- 验收（必须实际执行并贴输出）：
  1. 起 8002 验证实例（**用户的 8001 现役实例绝不能动**）：临时 `AUTH_ENABLED=1` 环境变量启动，curl `/api/audit/recent` 应 401/403；恢复默认关后再 curl 应正常返回——两个状态都贴输出，验完杀实例；
  2. `grep -n "test_llm_call\|question_for_prompt" backend/services/chat_service.py` 无该函数残留（注意 340 行主流程的 question_for_prompt 变量是正常代码，别误伤）；
  3. 前端 `npm run dev` 编译无报错；`grep -rn "测试大模型\|test-llm" frontend/src` 清零；
  4. py_compile 通过；进程清零。
- 禁止：不动 8001 现役实例；不动其他路由；不重写 CHANGELOG 旧条目；禁止 git commit/add。
- 完成后输出：①改动清单 ②验收输出（含 401/放行双状态） ③三句话总结。
