# 卡 #8（RAG）：代码节流提速——LLM 调用次数减半 + 快速失败
- 背景（2026-09-23 实测诊断）：不限流时同题 20.2s 基线（用户现役服务实测）；限流时最坏 2 分钟。根因=两层重试叠加（openai SDK 自动重试×2 + chat_service 自身 3 次重试）× Critic 最多 3 轮重检索（每轮=检索+LLM 评估+再生成）。用户已拍板"充值智谱（其自行操作）+ 代码节流"。
- 目标：①Critic 默认轮次降为 1（配置可调回）；②chat_service 层重试收紧为最多 1 次重试、总等待可控；③最坏情况 30 秒内出明确反馈（卡#7 的 error 事件通道），不限流基线不回退。
- 涉及文件：`backend/config/settings.py`（新增/启用配置项）、`backend/services/chat_service.py`（重试与 Critic 轮次逻辑）、`CHANGELOG.md`（v3.11）。
- 实现要点：
  1. **先读现状**：settings.py 是否已有 Critic 轮次/自审开关（CHANGELOG v3.4 提过"自审思考默认关"，可能已有配置体系，能复用就不新造）；chat_service 的重试循环与 Critic 轮次写死位置；
  2. Critic：新增/修改 `CRITIC_MAX_ROUNDS`，**默认 1**（保留最多轮次的配置能力，注释写明 1=速度优先 3=质量优先）；确认轮次=1 时拒答兜底逻辑不受影响（拒答判定走第一轮评估，读代码验证）；
  3. 重试：chat_service 大模型调用重试 3 次→1 次（首次失败后等 1.5s 重试一次，再失败立即走卡#7 的 error 事件快速结束）；openai SDK 客户端的 max_retries 若代码里可配则设为 1（两层重试相乘是最坏时长元凶）；禁止动流式正常路径；
  4. 极简：不加新依赖、不动检索/Critic 判定逻辑本身、不加"动态限流感知"等花活。
- 验收（必须实际执行并贴完整输出）：
  1. 正常回归：起服务问一题（服务端口 8001，若用户实例在跑则用 curl 直测现役实例前先确认代码改动需重启——**改动后须重启自己起的实例验证，绝不能杀用户开着的服务；若 8001 被占用则以 8002 起验证实例，验证完杀掉**）：流式正常出答案，贴总耗时（应与 20s 基线同量级）；
  2. Critic 轮次生效：日志应只出现"第1轮评估"，不再有第2/3轮（贴日志行）；
  3. 快速失败：base_url 临时指向无效端口（用 .env 注入法，用完复原 diff 确认），从提问到收到 error 事件总耗时 ≤30s（贴计时）；对比节流前同场景最坏 2 分钟；
  4. `py_compile` 通过；验完杀干净自己起的进程。
- 禁止：不动检索/改写/rerank/拒答判定逻辑；不动卡#7 的 error 事件协议；不加新依赖；禁止 git commit/add。
- 全局约束：外科修改；CHANGELOG 接续 v3.11。
- 完成后输出：①改动文件清单+配置项说明 ②三项验收完整输出（含计时） ③三句话总结。
