# 卡 #7（RAG）：LLM 限流/故障时前端友好降级提示（消灭无限转圈）
- 背景（2026-09-23 09:05 用户实测）：问"我应该玩决斗还是哨位"时前端一直转圈。日志根因：智谱 429（1302 账户速率限制 / 1305 模型访问量过大）+ 500 交替，重试 3 次失败；检索与 Critic 正常。**外部限流是常态，代码缺降级提示。**
- 目标：LLM 生成环节失败（429/5xx/超时）时，SSE 推送明确错误事件，前端停止 loading 动画并显示"模型服务繁忙，请稍后重试"类提示，不再无限转圈。
- 涉及文件：
  - `backend/services/chat_service.py`（大模型调用失败链路，日志已有"大模型调用失败(第N次)/流式大模型调用失败"）
  - SSE 输出处（chat_router 或 chat_service 的流式生成器——先读现有 SSE 事件协议）
  - `frontend/src/components/ChatPage.vue`（SSE onmessage 处理 + loading 状态）
- 实现要点：
  1. **先读后改**：读 chat_service 生成链路 + 前端 SSE 消费代码，摸清现有事件类型（如 token/done/error——以实际代码为准）；
  2. 后端：在重试耗尽/流式失败的 except 分支，yield 一条结构化错误事件（如 `{"type":"error","message":"模型服务繁忙，请稍后再试"}`），分类文案：429→繁忙稍后再试；5xx/超时→服务异常稍后再试；然后正常结束流（不发残缺答案）；
  3. 前端：收到 error 事件→停止 loading 转圈、渲染一条 assistant 错误气泡（样式随暗色主题）；连接中断（SSE onerror）同样停止 loading 并提示，杜绝无限转圈；
  4. 不加重试队列/不自动二次提问（YAGNI）。
- 验收（怎么算做完）：
  - 真实 429 不可控，用**本地模拟**：临时把 LLM base_url 指向本机无效端口（改环境变量或临时配置，**验完必须复原**），起服务问一题，curl SSE 流应看到 error 事件 JSON 且连接正常结束（贴完整 SSE 输出）；
  - 前端部分代码走查：贴 error 事件→停止 loading→渲染错误气泡的代码段，三处状态流转自查（loading=false 必达）；
  - 正常问答回归：恢复配置后起服务问一题，流式正常（贴日志尾部 200 OK）；
  - 验完杀干净进程。
- 禁止：不动检索/改写/rerank；不动 Critic 逻辑本身；不加新依赖；禁止 git commit/add。
- 全局约束：外科修改；CHANGELOG 接续版本号补条目（当前最新 v3.9）。
- 完成后动作：贴模拟限流的 SSE 输出+正常回归日志；更新台账卡#7 行；三句话总结。
