# 卡 #6：工单与审计管理页（运营侧告别裸 API）
- 目标：新增一个管理页，运营能看工单列表/统计、处理/关闭工单、看最近审计记录，不再靠 curl。
- 涉及文件：
  - 新建 `frontend/src/components/AdminPage.vue`
  - 前端入口挂载（先核实 KnowledgeManage.vue 现有的进入方式——顶栏按钮或组件切换——照同样方式加"管理"入口；如用 vue-router 则加路由）
- 前置依赖：卡#4 已完成（避免 ChatPage.vue 编辑冲突；本卡尽量不动 ChatPage.vue）
- 实现要点：
  1. 页面三个区块，风格完全照 `KnowledgeManage.vue`（Element Plus，先读它对齐写法）：
     - 工单列表：表格列（状态/问题摘要/创建时间），状态筛选，行内"解决""关闭"按钮——全部调 `ticket_router.py` 现有接口（先读该文件确认路径与参数，不新造后端接口）；
     - 工单统计卡：调现有 stats 接口，展示待处理/已解决/已关闭计数；
     - 审计最近记录：调 `GET /api/audit/recent`（`backend/main.py:40-43`），表格展示时间/动作/摘要；
  2. 后端零改动（接口都有）；若个别字段缺失需要后端微调，先在返回摘要中说明理由再动；
  3. 页面顶部注明"本地运营演示用，鉴权由后续用户体系承担"。
- 验收（怎么算做完）：
  - 前端起服务，进入管理页：三个区块均渲染出真实数据（工单≥0条、审计≥1条——audit 目录已有 JSONL 实数据）；
  - 手动清单：筛选状态✓ 解决按钮生效（状态变化）✓ 关闭按钮生效✓ 审计表有数据✓；
  - 用户端对话页功能不回归（发一条消息正常）。
- 禁止：不加登录/权限；不动 ChatPage.vue 除非加入口必须的一行；不引入新依赖。
- 完成后动作：贴手动清单打勾结果（可配 0-2 张关键截图路径）；更新台账卡#6 行；返回 3 行以内摘要。
