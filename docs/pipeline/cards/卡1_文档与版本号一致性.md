# 卡 #1：文档与版本号一致性（防穿帮）
- 目标：README 更新记录补齐 v3.5/v3.6 并与 CHANGELOG 对齐；消灭界面/配置里过期的版本自述。
- 涉及文件：
  - `README.md`（更新记录节、项目结构节）
  - `CHANGELOG.md`（只读对齐源，不改）
  - `backend/config/domain_profiles/valorant.yaml`（custom_rules 中版本自述）
- 前置依赖：无
- 实现要点：
  1. 读 `CHANGELOG.md` 中 v3.5（工单闭环：问答→兜底→自动建工单→人工答案回流知识库）与 v3.6（前端领域切换）条目，在 README「更新记录」节补两行摘要并注明"详见 CHANGELOG.md"；
  2. README「项目结构」节核对数据目录描述：现役运行时数据在 `backend/data/`（chroma_db/chat_history/audit/tickets.db），根目录 `data/` 为废弃旧目录（卡#3 处理），README 描述以 backend/data 为准；
  3. `valorant.yaml` 的 custom_rules 里"版本"回复硬编码 v3.0——改成**不含具体版本号**的表述（如"当前版本见 README 更新记录"），避免再次过期。
- 验收（怎么算做完）：
  - `grep -n "v3.5\|v3.6" README.md` 有命中；
  - `grep -rn "v3\.0" backend/config/domain_profiles/valorant.yaml` 无命中；
  - README 更新记录两条新摘要内容与 CHANGELOG 对应条目语义一致（肉眼逐条比对）。
- 禁止：不改 CHANGELOG.md 正文；不动 backend/ 下任何 .py；不重写 README 其他章节；不动根目录 aoci*.txt。
- 完成后动作：跑上面两条 grep 命令贴输出；更新台账卡#1 行；返回 3 行以内摘要。
