# RAG 对话助手（无畏契约智能对话小助手）· 交付流水线台账

- 模式：D 跳步优化 ｜ 落盘目录：`docs/pipeline/` ｜ 创建：2026-09-23
- 当前状态：进行中
- 本轮范围（用户 09-23 勾选）：防穿帮速修包 + 反馈闭环 + 管理后台（容器化部署未勾选，不做）

## 一、在做（最多一张）

| 卡 | 执行者 | 开工时间 | 备注 |
|---|---|---|---|
| （空——全部卡完成，进入收尾） | | | |

## 二、待执行（按依赖顺序）

| 卡 | 标题 | 依赖 | 可并行 | 验收要点 |
|---|---|---|---|---|
| （空） | | | | |

## 三、做完（勾选区）

- [x] 卡#1 文档与版本号一致性（09-23：README 补 v3.5/v3.6 摘要与 CHANGELOG 对齐；项目结构树改指 backend/data；valorant.yaml 版本自述去硬编码改指 README；两条 grep 验收一命中一零命中，yaml 解析 OK）
- [x] 卡#2 会话刷新失忆修复+CORS 收敛（09-23：后端补 GET /api/chat/history 复用 load_history；前端 session_id 持久化 localStorage+挂载拉历史渲染；CORS 收敛为 localhost:5174 白名单（vite 实际端口核实值，非预设 5173），陌生来源预检 400；存读闭环 curl✓ vite 编译✓ 进程清零✓；CHANGELOG 补 v3.7）
- [x] 卡#3 旧目录与脚手架杂物清理（09-23：根 data/ 双核实后删（现役 backend/data/chroma_db 在用+旧目录 mtime 2026-07）；脚手架四文件删后零悬空；网址/start_test.log 归档 docs/attic/；/docs 200+vite 编译✓ 进程清零✓ git status 仅限清单）
- [x] 卡#4 反馈闭环最小流程（09-23：feedback_service+feedback_router 照 ticket 先例，POST /api/feedback 落库+同问答覆盖不重复（created→updated 仍 1 行）+GET recent；前端 👍/👎 按钮（成对上送/防连点/失败回滚/已评置灰）；L1 基线 Hit@5 70.6%/MRR 0.598/拒答 100% 改动前后零回归；端到端经 vite 代理✓；CHANGELOG 补 v3.8）

## 四、阻塞区

| 卡 | 阻塞原因 | 需要谁 |
|---|---|---|
| （空） | | |

## 备注
- 智谱 API"key 失效"为过时记忆，实为余额不足已解决（pit-004 已 resolved），不构成本轮阻塞。
- RAG 仓库有未提交改动（v3.5/v3.6 相关，CORE.md →work-094"未commit待用户定"）：各卡完成后的 git 提交动作统一等用户拍板，子代理只改不 commit。
